spring:
  datasource:
    url: jdbc:mysql://localhost:3307/family_assistant?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      password: 1234
      database: 0
      timeout: 3000ms

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

app:
  upload-dir: uploads
  file-base-url: /api/files
  cors-origins: http://localhost:*

jwt:
  secret: family-assistant-jwt-secret-key-2026-must-be-32bytes
